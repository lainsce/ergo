case COGITO_BUTTON:
      {
        int pad = 24;
        int min_w = 120;
        int min_h = 56;
        w = text_w + pad * 2;
        h = text_h + 16;
        if (w < min_w) w = min_w;
        if (h < min_h) h = min_h;
      }
      break;
    
